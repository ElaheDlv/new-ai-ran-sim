--window 192 --batch-size 32 --early-stop 30 --feature-sets length delta_t+length uniform-length --hidden-dim 128
--window 256 --batch-size 32 --early-stop 30 --feature-sets length delta_t+length uniform-length --hidden-dim 128
--window 256 --batch-size 32 --early-stop 30 --feature-sets length delta_t+length uniform-length --hidden-dim 256
--window 384 --batch-size 32 --early-stop 30 --feature-sets length delta_t+length uniform-length --hidden-dim 192
--window 384 --batch-size 32 --early-stop 30 --feature-sets length delta_t+length uniform-length --hidden-dim 256
--window 512 --batch-size 32 --early-stop 30 --feature-sets length delta_t+length uniform-length --hidden-dim 256

--window 256 --batch-size 64 --early-stop 30 --feature-sets length delta_t+length uniform-length --hidden-dim 128
--window 384 --batch-size 64 --early-stop 30 --feature-sets length delta_t+length uniform-length --hidden-dim 192

--window 384 --batch-size 32 --early-stop 30 --feature-sets length delta_t+length uniform-length --hidden-dim 192
--window 512 --batch-size 32 --early-stop 30 --feature-sets length delta_t+length uniform-length --hidden-dim 256
--window 256 --batch-size 32 --early-stop 30 --feature-sets length delta_t+length uniform-length --hidden-dim 128
--window 384 --batch-size 32 --early-stop 30 --feature-sets length delta_t+length uniform-length --hidden-dim 192


# --- SMALLER, SHORT-RANGE CONTEXT (mostly for URLLC; also a baseline sanity check) ---
--window 64  --batch-size 32 --early-stop 30 --feature-sets length delta_t+length uniform-length --hidden-dim 128
--window 96  --batch-size 32 --early-stop 30 --feature-sets length delta_t+length uniform-length --hidden-dim 128
--window 128 --batch-size 32 --early-stop 30 --feature-sets length delta_t+length uniform-length --hidden-dim 128
--window 160 --batch-size 32 --early-stop 30 --feature-sets length delta_t+length uniform-length --hidden-dim 192

# Stability variants (same windows, larger hidden or batch)
--window 96  --batch-size 64 --early-stop 30 --feature-sets length delta_t+length uniform-length --hidden-dim 192
--window 128 --batch-size 64 --early-stop 30 --feature-sets length delta_t+length uniform-length --hidden-dim 192

# If you want a pure-Length ablation at small windows (to measure Δ from adding delta_t)
--window 128 --batch-size 32 --early-stop 30 --feature-sets length delta_t+length uniform-length --hidden-dim 128
